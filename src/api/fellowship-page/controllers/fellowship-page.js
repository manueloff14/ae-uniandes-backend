'use strict';

/**
 * fellowship-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::fellowship-page.fellowship-page');
